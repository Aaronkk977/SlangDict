{
  "name": "slang-dict",
  "private": true,
  "scripts": {
    "build": "cd web && npm run build",
    "vercel-build": "cd web && npm install && npm run build",
    "dev": "cd web && npm run dev"
  }
}
